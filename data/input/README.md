# Input

Place the authoritative Royal Glass `.xlsx` workbook for the Phase 4 validator in this folder. The first build will select only its **Needs Validation** records; it will preserve original values and provenance.
